# Mesa de ayuda — aviso de carpeta

Esta subcarpeta **no** es la raíz del proyecto Node (aquí no está `package.json` ni `server.js`).

**Instrucciones completas:** abra el README de la raíz del repositorio:

- Ruta en disco: `../README.md` (un nivel arriba respecto a este archivo).
- Enlace relativo en GitHub o en el IDE: [README principal](../README.md).

Instale dependencias y ejecute el servidor **desde la carpeta raíz** del repo.
